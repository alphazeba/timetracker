[package]
name = "time-tracker-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "tt"
path = "src/main.rs"

[dependencies]
time-tracker-lib = { path = "../time-tracker-lib" }
clap = { version = "4", features = ["derive"] }
chrono = { version = "0.4", features = ["serde"] }
owo-colors = "4"
